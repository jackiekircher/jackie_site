%h1
  #{@title}
%aside
  %h2
    archives

%article.archive
  %ul.archive
    - latest_posts.each do |post|
      %li
        %a{:href => "/posts/#{post.name}", :title => post.title, :alt => post.title}
          = post.title
          %div.date
            = post.formatted_date


